/*
using UnityEngine.SceneManagement;
using NUnit.Framework;

[TestFixture]
public class BackToMainMenuTests
{
    [Test]
    public void GoToCardGuide_SceneLoaded()
    {
        // Arrange
        var backToMainMenu = new BackToMainMenu();

        // Act
        backToMainMenu.GoToCardGuide();

        // Assert
        Assert.That(SceneManager.GetActiveScene().name, Is.EqualTo("HelpScenes"));
    }

    [Test]
    public void BackToMain_SceneLoaded()
    {
        // Arrange
        var backToMainMenu = new BackToMainMenu();

        // Act
        backToMainMenu.BackToMain();

        // Assert
        Assert.That(SceneManager.GetActiveScene().name, Is.EqualTo("StartScenes"));
    }
}
*/